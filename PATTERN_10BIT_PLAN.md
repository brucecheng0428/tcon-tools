# Pattern Generator 10bit 輸出 — 規劃與實作記錄（縮減版）

- 對象檔案：`pattern.html`
- 版本：v3.2.0 → v3.3.0（BMP）→ **v3.4.0（PNG）**
- 狀態：**BMP 與 PNG 皆已實作並驗證完成**
- 最後更新：2026-08-08

---

## 0. 需求本質（2026-08-07 Bruce 收斂）

> 「用 10-bit 其實很單純，就是因為**有些 pattern（配單卷）它只吃 10-bit 的圖片**。」
> 「我們有時候要匯出可能像是 **subpixel on/off** 的圖片，它**暗是 L0、亮是 L1023**。像這種的其實**精準度不用太高，只要最亮跟最暗是對的就好**。」
> 「單純只是為了**有些機臺它只吃 10-bit、不吃 8-bit**，我們要匯出圖片給這個機臺用而已。」

**需求本質是「產生機台吃得下的 10bit 檔案」，不是「產生 8bit 做不到的灰階」。**

這一句話讓整個工程量縮掉九成，理由見 §1。

---

## 1. 定案的設計（Bruce 已裁示，不再更動）

| # | 決策 | 直接後果 |
|---|------|---------|
| 1 | **輸入維持 8bit（0~255），不做 10bit 輸入** | `pgRgb` 的進位污染不用處理；`box`/`clear`/`pgPixels` 不用換；34 種 pattern、`pgDrawPattern` 273 行**一行都沒動**；canvas 維持 8bit，anti-aliasing 的坑自然消失 |
| 2 | 換算用 **MSB replication** `v10 = (v8 << 2) \| (v8 >> 6)` | 127→509；🔴 **兩端精準是硬需求**：0→0、255→1023 |
| 3 | **先做 BMP，後做 PNG** | v3.3.0 交付 BMP（當時 PNG 選 10bit 直接禁用，不讓使用者拿到假的 10bit 檔）；v3.4.0 補上 PNG，禁用解除 |
| 4 | BMP 用**最標準、相容性最好**的形式 | 32bpp + `BI_BITFIELDS`，A2R10G10B10，`biSize=40`（非 V4/V5） |
| 5 | PNG 的 P1/P2 —— **已於 v3.4.0 重新推導後決定用 P2**（見 §5.1） | 不是沿用舊決定：關鍵理由是「多數讀取器不理會 `sBIT`」，P1 會讓全白在那些讀取器眼中變成 1.56%（幾乎全黑），正好毀掉「最亮最暗要對」這個唯一需求 |

> 🔴 **§1 的決策 1 使原規劃的甲／乙／丙三案架構比較全部作廢。** 那份分析保留在 §7 作為決策軌跡。

---

## 2. 實際產出的檔案結構（給機台對格式用）

這是 `pgEncodeBMP32_10()` 寫出來的**實際**位元組佈局，每個數字都經過實測驗證（見 §4）。萬一機台不吃，直接對照這張表調。

```
偏移      長度  內容
──────────────────────────────────────────────────────────────
[0]        2   'BM' (0x42 0x4D)
[2]        4   bfSize        = 66 + W*H*4
[6]        4   bfReserved1/2 = 0
[10]       4   bfOffBits     = 66          ← 像素資料起點
──────────────────────────────  BITMAPINFOHEADER（40 bytes）
[14]       4   biSize        = 40          ← 刻意用 40，不是 V4(108)/V5(124)
[18]       4   biWidth       = W
[22]       4   biHeight      = +H          ← 正數 → bottom-up（最後一列先寫）
[26]       2   biPlanes      = 1
[28]       2   biBitCount    = 32
[30]       4   biCompression = 3           ← BI_BITFIELDS
[34]       4   biSizeImage   = W*H*4
[38]       4   biXPelsPerMeter = 2835      （72 dpi）
[42]       4   biYPelsPerMeter = 2835
[46]       4   biClrUsed     = 0
[50]       4   biClrImportant= 0
──────────────────────────────  三個遮罩（12 bytes）
[54]       4   Red   mask = 0x3FF00000     （bit 20-29）
[58]       4   Green mask = 0x000FFC00     （bit 10-19）
[62]       4   Blue  mask = 0x000003FF     （bit 0-9）
──────────────────────────────
[66]     W*H*4 像素，每點 1 個 DWORD，little-endian
               位元配置：[31:30]=alpha  [29:20]=R  [19:10]=G  [9:0]=B
               alpha 一律 = 3（二進位 11）
```

### 幾個刻意的選擇與理由

| 項目 | 選擇 | 理由 |
|------|------|------|
| DIB header | `biSize=40` + 3 個遮罩 | 這是 `BI_BITFIELDS` 最原始、相容性最廣的寫法。`BITMAPV4HEADER`(108) 把遮罩放進 header 內，有些舊讀取器不吃 |
| alpha 2 bit | 填 **3**（不透明） | `biSize=40` 的 BI_BITFIELDS 只定義 RGB 三個遮罩，這 2 bit 嚴格說是未定義的。但若讀取器把它當 alpha 解讀，填 0 會變成全透明（整張圖可能顯示成空白），填 3 才安全。**兩種解讀下填 3 都不會錯** |
| 列順序 | bottom-up（`biHeight` 正數） | 沿用既有 24bpp 編碼器，一致 |
| padding | **無** | 32bpp 每列 `W*4` bytes 天然 4-byte 對齊（24bpp 才需要補） |

### 🔧 若機台不吃，可調的旋鈕（依可能性排序）

1. **改 top-down**：`biHeight` 寫 `-H`，並把列順序反過來。程式裡只有兩處要改。
2. **alpha 改填 0**：若機台把最高 2 bit 當保留位且要求為 0。
3. **改用 BITMAPV4HEADER**：`biSize=108`，遮罩移進 header，另加 alpha mask `0xC0000000` 與 CSType。
4. **改通道順序**：目前是 A2**R**10**G**10**B**10；若機台要 A2B10G10R10，把 R/B 兩個遮罩對調即可。

---

## 3. 實作內容

**改動檔案只有三個**（他人未 commit 的 7 個檔案與 `docs/_tmp_*` 全程未觸碰）：

| 檔案 | 改動 |
|------|------|
| `pattern.html` | +184/−24 行 |
| `common/i18n.js` | +5 個 key（三語） |
| `common/version.js` | `pattern: v3.2.0 → v3.3.0` |

### `pattern.html` 的改動清單

| 項目 | 內容 |
|------|------|
| UI | 「檔案格式」下方新增「色彩深度」radio group（8 bit / 10 bit），**預設 8 bit** |
| `pg8to10(v)` | MSB replication，新增 |
| `pgEncodeBMP32_10()` | 新的 32bpp BITFIELDS 編碼器，新增 |
| `pgVerifyBmp10()` | **不走 `createImageBitmap`** 的位元組級驗證器，新增 |
| `pgVerifyRoundTrip()` | 加第 5 個參數 `depth`；**未傳或傳 8 時走原本那條路，一個位元都沒變** |
| `pgOutDepth()` / `pgOutDepthAvail()` / `pgOutSyncDepthUI()` | 深度選擇與 PNG 時的禁用邏輯，新增 |
| `pgOutBuild()` | 依 depth 分派到兩個 BMP 編碼器 |
| `pgOutSave()` / `pgOutShare()` / `pgOutBatch()` | 一路把 depth 帶下去（批次匯出同樣支援 10bit） |
| `pgExportBaseName()` | 10bit 時檔名自動加 `_10bit` 後綴 |
| `pgOutUpdate()` | 檔案大小估算改依 depth；tag 顯示 `BMP 10bit` |

### PNG + 10bit 的處理

PNG 沒有原生 10bit（規格只允許 1/2/4/8/16），要做得靠 16bit + `sBIT`，而且 `canvas.toBlob` 永遠只吐 8bit、必須自寫編碼器 —— 那是下一階段。

**在那之前，選 PNG 時 10bit 選項直接 disabled**（並顯示原因），而不是默默存出 8bit 檔。理由：讓使用者以為拿到 10bit 檔、實際拿到 8bit，是這個功能最糟的失敗模式 —— 機台那端不會報錯，只會結果不對。

---

## 4. 驗證結果（全部實測，非宣稱）

環境：Chrome 150，本機 `http://127.0.0.1:8765/pattern.html` 服務**改動後的本地檔案**。

### 4.1 🔴 8bit 路徑位元組零差異（回歸底線）

改動**前**先錄基線，改動**後**用完全相同的設定重跑，比對 sha256：

- 11 組設定 × 2 種格式（BMP24 + PNG）= **22 個 sha256，全部完全相同**
- 涵蓋：純色 L0/L127/L255、色帶、pixel 棋盤、含 anti-alias 的 `aligncenter` 與 `character`、`smpte`、`lum_compare`、反相漸層 `colortest`
- 含 **641×361 奇數尺寸**（專門驗 24bpp 的列 padding 沒被動到）

基線檔：`baseline_8bit.json`（記於工作資料夾）

### 4.2 端點精準（Bruce 指定的硬需求）

| 測項 | 結果 |
|------|------|
| 全黑 L0 | R=0 G=0 B=0，raw = `0xC0000000` ✅ |
| 全白 L255 | R=1023 G=1023 B=1023，raw = `0xFFFFFFFF` ✅ |
| alpha | 兩者皆為 3 ✅ |

### 4.3 0~255 全值域掃描

逐一產檔並讀回檔案裡的實際 10bit 值：

- 與 MSB replication 公式**不符者 = 0 / 256**
- 映射後**嚴格遞增**（不會有兩階併成同一個值）
- 取樣：`0→0`、`1→4`、`127→509`、`128→514`、`254→1019`、`255→1023`

### 4.4 內建驗證器（位元組級，非 createImageBitmap）

在 641×361（奇數寬）上：`diff = 0 / 231,401 px`。

🔴 **負向對照 —— 證明驗證器不是永遠回傳成功**：

| 動作 | 驗證器反應 |
|------|-----------|
| 故意改壞像素區 1 個 byte | `diff = 1`（精準抓到 1 點） |
| 故意改壞 Green 遮罩 | 直接以 header 檢查擋下，回報 `mask 3ff00000/12345/3ff` |

### 4.5 真實使用者路徑

實際按「另存圖片」，畫面驗證框顯示：

> ✅ 已存檔並自動驗證（BMP 10-bit，256.1 KB）：把檔案解碼回來與來源逐像素比對，共 65,536 px，**差異 0**。

### 4.6 第三方工具相容性（不是用自己的解碼器自我驗證）

| 工具 | 結果 |
|------|------|
| **Python**（自行解析 header + 遮罩） | 全部欄位符合 §2 表；`subpixel on/off` 檔的相異色**只有 2 種：(0,0,0) 與 (1023,1023,1023)** —— 正是 Bruce 要的「暗 L0、亮 L1023」 |
| **ImageMagick** | 四個檔案全部正常開啟，識別為 `BMP3 32x32 ... 32-bit` |
| **macOS 預覽程式** | 正常開啟並正確顯示漸層與棋盤（截圖存證） |

### ⚠ 一個要告訴 Bruce 的實測發現

**ImageMagick 把全白解出來是 254.755/255（99.9%），不是 100%。**

原因：它把 10bit 值用 `v/1024` 正規化，而不是 `v/1023`。**我們的檔案本身是對的** —— 直接讀位元組確認就是 1023（10bit 的最大值），這點已由 Python 獨立驗證。

但這代表**不同讀取器對 10bit 滿刻度的解讀不一致**。如果機台那端也用 `v/1024`，全白會被讀成 99.9% 而非 100%。這是格式外部的解讀差異，我們無法在檔案裡消除；若機台真的在意，要靠 §2 的旋鈕或與機台廠確認正規化方式。

---

## 5. 10 bit PNG（v3.4.0，2026-08-08）

### 5.1 P1 / P2 的決定：**P2（放大到滿刻度）＋ `sBIT = 10`**

PNG 沒有原生 10 bit（規格只允許 1/2/4/8/16），所以 10 bit 只能用 **16 bit + `sBIT=10`** 表達。
問題在於：那 10 bit 的值要放在 16 bit 容器的哪裡？

| 方案 | 做法 | 全白在**不理會 sBIT** 的讀取器眼中 |
|------|------|--------------------------------|
| P1 | 1023 直接放在低位 | `1023/65535 ≈ **1.56%**` → **全白變成幾乎全黑** |
| **P2（採用）** | 放大到滿刻度 `1023 → 65535` | `65535/65535 = **100%**` → 正確 |

**決定用 P2，理由是「多數讀取器不理會 `sBIT`」**：

- Bruce 的核心需求是「**最亮跟最暗要對**」（subpixel on/off，L0 / L1023）
- P1 會讓不理 sBIT 的讀取器把全白讀成 1.56%，**正好毀掉他唯一在意的那件事**
- P2 則兩種讀取器都不會錯得離譜：不理 sBIT 的讀到滿刻度（正確）；理會 sBIT 的用 `v16 >> 6` 可**精準還原回 0~1023**（實測 1024 個值零失敗）

【實測佐證】同一支 ImageMagick（不理會 sBIT）：

| 檔案 | 全白讀出來 |
|------|-----------|
| 本次的 16bit PNG（P2） | **100%** ✅ |
| 上一輪的 10bit BMP | 99.9039%（它用 `v/1024` 正規化） |
| 若採 P1 | 1.56% ❌ |

> 🔧 **剩下的唯一風險與判別方式**：若機台**指名要在 16 bit 容器裡讀到 1023**，那它要的是 P1。
> **拿去測時要看的現象：如果機台讀出來是 ~1.5% 或整張近乎全黑，就是它要 P1** ——
> 那只是把 `pg10to16()` 改成直接回傳 v10（一行），改起來是小事。

### 5.2 換算

```
v8  → v10 = (v8 << 2) | (v8 >> 6)          （沿用已定案的 MSB replication）
v10 → v16 = round(v10 × 65535 / 1023)
```

整數實作用 `(v * 65535 + 511) / 1023` 取整，避開浮點。【實測】1024 個值與
`Math.round(v*65535/1023)` **完全一致**；`0→0`、`1023→65535` 兩端精準；`v8` 0~255 映射後嚴格遞增。

⚠ 注意這**不等於** 10→16 的 bit replication（`(v<<6)|(v>>4)`），兩者有 234/1024 個值不同。
本專案照 Bruce 指定的 round 公式。

### 5.3 檔案結構

```
89 50 4E 47 0D 0A 1A 0A          PNG 簽章
IHDR(13)  width / height / bitDepth=16 / colourType=2(truecolor) / 壓縮0 / 濾波0 / 交錯0
sBIT(3)   10, 10, 10             🔴 規格要求必須在 PLTE 與 IDAT 之前
IDAT(n)   zlib(RFC1950) 壓縮；每條 scanline 前一個 filter type byte（用 0 = None）
IEND(0)
```

- 🔴 **16 bit sample 是 network byte order（big-endian，MSB 先）** —— 規格明文，最容易寫錯的地方
- 每個 chunk 都有 CRC32（涵蓋 type + data）
- IDAT 用 `CompressionStream('deflate')`，實測 Chrome 150 吐的是 zlib(RFC1950)（開頭 `78 9C`），正是 PNG 需要的；`deflate-raw` 是裸 deflate，不能用

### 5.4 檔案大小（實測）

1920×1080 未壓縮是 `1920*1080*6 = 12,441,600 B ≈ 12.4 MB`，經 deflate 後：

| 內容 | 實際檔案 | 壓縮比 | 編碼耗時 |
|------|---------|--------|---------|
| subpixel on/off（1920×1080） | **27,334 B** | 455:1 | 186 ms |
| 256 階漸層 horiz256（1920×1080） | **85,125 B** | 146:1 | 159 ms |
| SMPTE（512×288） | 9,627 B | 92:1 | 27 ms |

pattern 圖高度規律，壓縮率很高，12.4 MB 的未壓縮基準不會實際發生。

### 5.5 尚未做的事

| 項目 | 狀態 |
|------|------|
| 螢幕 1:1 檢視 / 放大鏡 | 未改。螢幕本來就是 8bit，維持現狀正確 |
| sub-pixel 編輯器 / 匯入圖 | 未改。輸入維持 8bit，存 10bit 時一樣走 `pg8to10` 放大 |

---

## 6. 版號判定

**`pattern` v3.2.0 → v3.3.0（MINOR）**

**判定依據：** 依 `docs/VERSIONING.md` §2 案例 1「新增一個完整功能」—— 新增「10 bit BMP 輸出」，使用者**多了一件原本做不到的事**（產生只吃 10-bit 的機台能讀的檔案）。既有操作全部在原位、預設值仍是 8 bit、8 bit 的輸出結果經 22 個 sha256 證明**位元組完全相同** → 不觸發 MAJOR，也不是 PATCH。輸出結果對既有使用方式不變，**不加 `⚠ 輸出變更`**。

同時涉及 §2 案例 13（修改共用檔 `common/i18n.js`）：本次只**新增** `pat.outDepth*` 五個 key，未更動任何既有 key，其他分頁不使用這些 key，故其餘分頁不受影響、不進版、不動其 cache buster。

cache buster：`pattern.html` 的 `version.js` / `i18n.js` 查詢字串同步改為 `?v=20260808pat330`。

---

## 7.（已作廢，保留決策軌跡）原規劃的架構分析

> 🔴 **已作廢：Bruce 2026-08-07 收斂為 8bit 輸入。**
> 以下內容是在「要做真 10bit 輸入、要能做出 L=513 這種 8bit 表達不出來的階」前提下的分析。
> 該前提已被推翻 —— 需求是「餵機台」而不是「做 8bit 做不到的階」，因此**甲／乙／丙三案全部不需要**，
> `pgDrawPattern` 273 行與 34 種 pattern 一行都沒有改動。
> 保留這一節是為了記錄「為什麼曾經以為要大改」以及當時實測到的事實。

### 7.1（作廢）`pgRgb` 的進位污染

【已實測】`pgRgb` 用 `PG_MULT[idx] * lv` 把 level 塞進 byte lane（`0x010101 × level`），這個技巧只在 `lv ≤ 255` 成立：

| level | `0x010101 × level` | 取出的 RGB |
|-------|-------------------|-----------|
| 255 | 0xffffff | [255,255,255] ✅ |
| 513 | 0x2030301 | [1,3,3] ✗ |
| 1023 | 0x40302ff | [255,2,3] ✗ |

**作廢原因**：輸入維持 8bit，`lv` 永遠 ≤ 255，此路徑不會被觸發。

（當時已驗證的替代方案：改用通道遮罩 `PG_CHMASK`，窮舉 8 色 × 256 階與現行實作 0 差異。**本次未採用，也未改動 `pgRgb`。**）

### 7.2（作廢）乙案：索引 pass / palette

【已實測】否決理由不是色階數量，而是 anti-aliasing：

| pattern | 邏輯應有色數 | 實測色數 | 說明 |
|---------|------------|---------|------|
| `character`（charMode=0） | 2 | **142** | 文字 AA |
| `aligncenter` | 2 | **5** | 橢圓 AA（值為 0, 64, 128, 191, 255） |

canvas 在 AA 邊緣會把相鄰的 ID 混色，混出來的數字既不是 A 也不是 B 的 ID，查表必然失敗。

**作廢原因**：canvas 維持 8bit，AA 混出來的中間值照 `pg8to10` 規則放大即可，本來就正確，不需要 palette。

### 7.3（作廢）甲案／丙案

- 甲案：改寫全部 273 行幾何邏輯寫進 `Uint16Array`。風險最高。
- 丙案：把 `box`/`clear`/`pgPixels` 三個原語做成雙後端，幾何碼不動。

**作廢原因**：兩者都是為了「讓 pattern 本身產生 10bit 數值」而存在。需求既然只要求輸出格式是 10bit、數值由 8bit 放大即可，這一層完全不需要。

### 7.4（仍然有效）舊驗證器在 10bit 下會失效

【已實測】這一條**沒有作廢**，而且正是本次 `pgVerifyBmp10` 存在的理由：

走 `createImageBitmap` 讀 10bit 內容時，Chrome 會塞回 8bit canvas：

| 原始 10bit | 讀回的 8bit |
|-----------|------------|
| 512 / 513 / 514 / 515 | **128 / 128 / 128 / 128（全糊成同一個）** |
| 1020 / 1021 / 1022 / 1023 | **255 / 255 / 255 / 255** |

所以 10bit 一律走位元組級驗證。

### 7.5（作廢）10bit 輸入的 8↔10 換算與數值保存策略

當時規劃過「內部恆存 10bit、8bit 模式夾住顯示、手動編輯才覆蓋」的方案，並實測出：

- 8 → 10 → 8 無損（256 個值零失敗）
- 10 → 8 → 10 有 **768/1024（75%）會漂掉**（L=513 切一次 8bit 就永久變 514）

**作廢原因**：不做 10bit 輸入，不存在 10→8 的截斷問題。

### 7.6（更正）pattern 數量

原簡報寫「45 種圖案」，【已實測】`PG_PATTERNS` 註冊表實際是 **34 個 id**。
